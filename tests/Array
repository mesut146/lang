class Array{

  func copy<T>(arr: T[]){
    var res: T[arr.length];
    copy(arr, 0, res, 0, arr.length);
    return res;
  }

  func copy(src: T[], srcOff: int, trg: T[], trgOff: int, count: int){
    for(var i = 0;i < count;i++){
      trg[trgOff] = src[srcOff + i];
    }
  }

  func a(){

  }

}