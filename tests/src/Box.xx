class Box<T>{
    val: T*;
}

impl Box<T>{
    fn new(val: T): Box<T>{
        let ptr = malloc<T>(1);
        *ptr = val;
        return Box{val: ptr};
    }

    fucn get(self): *T{
        return self.val;
    }

    func unwrap(self): T{
        return *self.val;
    }
}