class Node<T>{
  next?: Node<T>;
  value: int = 0;
}

enum Color{
  WHITE,
  BLACK
}

class a{
  func calc();
}


func calc(a:int,b:int,carry?:int) : int
{
  if(carry){
    return a + b + carry;
  }
  else{
    return a + b;
  }
}

func def(a:int = 0);

calc(1,2);
calc(1,2,3);
def();
def(5);