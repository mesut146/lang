search_dir=/usr/lib/llvm-16/lib

look=$1

for lib in $search_dir/*.a
do
  #echo "lib=$lib"
  str=`nm --defined-only $lib | c++filt`

  if [[ $str == *$look* ]]; then
    echo "Found in $lib"
  fi

done
