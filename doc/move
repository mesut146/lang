let {a, moved} = (A::new(), false);

for(;;){
    if(c){
        let a = A::new();
        if(c1){
            let b = A:new();
            if(c2){
                //Drop::drop(a)
                continue;//causes to drop of b & a
            }
        }else{
            break;
        }
        //Drop::drop(a)
    }
}

