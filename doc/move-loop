//while
let v = get();
while(c){
  if(c2){
    send(v) //this also invalid, maybe second iter
  }
  else{
    //valid at first but not next iters
  }
  //invalid
}
//invalid

else reachable from if, bc they are in loop