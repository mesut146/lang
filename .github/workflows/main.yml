
name: test

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        lang-version: [ '1.5' ]

    steps:
      - name: Install LLVM and Clang
        run: wget https://github.com/mesut146/lang/releases/download/llvm19/libLLVM.so.19.1
      
      - uses: actions/checkout@v4
      - name: myname
        run: |
          ls
          # bash cpp_bridge/x.sh
          cd bin
          # wget https://apt.llvm.org/llvm.sh && chmod +x llvm.sh && sudo  ./llvm.sh 19
          x86-64/x-x64 bt x2-x64